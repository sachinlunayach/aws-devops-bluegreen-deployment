version: 0.2

phases:
  install:
    commands:
      - echo Installing dependencies (if any)...

  build:
    commands:
      - echo Building static website...
      # Add any build tools like npm run build, etc. (optional)

  post_build:
    commands:
      - echo Post-build stage...
      - echo Creating deployment package
      - zip -r website.zip .  # Zips all website files

artifacts:
  files:
    - website.zip
    - appspec.yml
